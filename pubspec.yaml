name: flutter_lua_bridge
description: "Dart <-> Lua interoperability. Compile from source code, easy to replace Lua core."
version: 0.0.1
homepage: https://github.com/yang-lile/flutter_lua_bridge
repository: https://github.com/yang-lile/flutter_lua_bridge

environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.1.3
  code_assets: ^1.0.0
  hooks: ^1.0.1
  native_toolchain_c: ^0.17.4

dev_dependencies:
  build_runner: ^2.4.13
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.25.8

platforms:
  android:
  ios:
  macos:
  linux:
  windows:

topics:
  - lua
  - interop
